{
  "pattern": "^(main){1}$|^(b|f|h|r)/(\\d+).+$",
  "errorMsg": "Branch name must start with `b/`(bugfix), `f/`(feature), `h/`(hotfix), `r/`(refactor) and be followed by `xxx` where xxx is the issue number"
}
